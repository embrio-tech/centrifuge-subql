const poolCreatedEvent = {
  block: { timestamp: new Date() },
  event: {},
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
} as any

describe('Given an created pool event,', () => {
  poolCreatedEvent
  //test()
})
